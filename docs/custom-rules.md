# Custom Rules

The file `.claude/rules/code-writing.md` contains rules, best practices
and conventions that MUST be **strictly** followed when writing Python
code in this repository. Claude Code loads it automatically as project
instructions, so it applies to every Python change without needing to
be requested.
